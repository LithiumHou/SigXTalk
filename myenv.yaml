name: SigXTalk_py  # Replace with the name of your environment
channels:
  - conda-forge       # Specify the channels to use
  - defaults          # Defaults channel as a fallback
dependencies:
  - python=3.8        
  - numpy=1.24.3
  - pandas=2.0.3
  - scikit-learn=1.3.0
  - argparse=1.4.0
  - scipy=1.10.1
  - pip               # Include pip if you need pip-installed packages

